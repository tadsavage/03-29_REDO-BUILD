DONE >> Save window - placed in middle and appearance made more consistent with rest of ui.
DONE >>Load window - placed in middle and appearance made more consistent with rest of ui. screenshot thumbnails.
DONE >>'Spent today' is not updating on deletetion of objects. (e.g. if you build a building and then delete it.
DONE >>Bring spent today along in load save or it screws up the accounting.
DONE >>Camera moves when i enter save name in save menu. Needs to be fixed.
DONE >>Quicksave / quickload functionality.
DONE >>ToastWindow for showing messages to the player (e.g. when they save, when they build something, etc.)
DONE >>Add a delay to the submenu so it doesnt disappear the second you move the mouse off of it.
DONE >> Fix Drag Cost preview window so it shows correct cost and follows ray in screen space.
DONE >> $ popups over obects in the world. (e.g. when you hover over a building, it shows the name and cost of the building)
DONE >> (Cancelled - not necessary) Small window that shows building icon, cost, maintenance cost, and descrition then you over over an item button in the build menu.
DONE >> Rytech door created and added to game.
DONE >> Equipment door safety barriers created and added to game.
DONE >> Add a female shipping clerk to the game
DONE >> Update animations for workers.
DONE >> Rats
DONE >> Need a Pest Control personnel type. (e.g. Exterminator, etc.)
DONE >> Mismatched colors on walls and floors in some areas.
		Make all match WALL COLOR and FLOOR COLOR in the color palette.
			Underside of top ledge of wall  
			Corner asset does not match wall color
			Underside of corner asset is missing a surface on inside right side of corner
			Light showing through Man door bottom ledge
DONE >> Undo is not Undoing deletions of objects. (e.g. if you build a building and then delete it, undoing should bring it back but it doesnt)
DONE >> Loading the game back in reverts items 'Moved' after initial placement back to 'Placed' and they lose their moved position.
DONE >> You can click through the UI - specifcally the Delete.
DONE >> Some players not moving on instantiating or popping across a few tiles when they are instantiated. Part of this was because one of the waypoints was up really high and couldnt be reached.
DONE >> Hover UI is too big and its stuck at the top of the screen. It should be smaller and follow the mouse cursor around so that it can be used to show information about objects in the world when you hover over them.
DONE >> Wrong prefabs being used for MHE
DONE >> Remove Scroll Zoom and make items rotate by using the scroll wheel while a preview object is active.
DONE >> Why cant I place 'Floor' objects under shipping doors or racks? I should be able to place floor objects anywhere.
DONE >> Shipping, Man and Rollup doors need to have scripts that allow the doors to open and close when a player is near them.
DONE >> RTS style Camera controls. (e.g. WASD to move, Q and E to rotate, etc.)
DONE >> Find a way to get nav agents to pathfind around objects that are in the way of their path. (e.g. if a worker is walking to a location and there is a building in the way, they should be able to pathfind around it instead of getting stuck.)
DONE >> Rework font and overall color theme of the UI to be more consistent and visually appealing. Currently the font and colors are a bit all over the place and could use some work to make them look more cohesive.
DONE >> Rework the entire UI to use Font X and a color pallete that is more consistent and visually appealing. Currently the UI is a bit all over the place and could use some work to make it look more cohesive and professional.
DONE >> Lower 'Ground' asset so that we can create foundation cubes. This will later help to create outside areas and make trucks able to back into the shipping doors.
DONE >> Shipping doors need Door Numbers added to them. (e.g. 1, 2, 3, etc.)
DONE >> Rack Locations need a placeholder for text on the location labels.
DONE >> Simplified Building Icons on the category bar.
DONE >> Case size Variety - currently all cases are the same size, but in reality there are many different sizes of cases. We should add a variety of case sizes to the game.
DONW>> Fix disappearing wall sections when we lower walls
DONE >> Finish Guard Shack and Gate Arm and Implement with animation and functionality for opening and closing when a player or any moving agent is near them.
DONE >> money audited and balanced. (e.g. cost of buildings, maintenance costs, etc.) Object cost review in Excel.
DONE >> Create a Security Guard employee type and add them to the game. They should be able to patrol the yard and keep an eye out for any suspicious activity.
DONE >> Create a Fenceobject and add it to the game. It should be able to be placed around the outside areas of the warehouse to keep out intruders and keep the yard secure.
DONE >> switchboard for all settings (e.g. music setting, sound effects setting, splash screen, speeds of things, etc.) Just go through all the empty objects in scene with serialized fields on them.
DONE >> Added Vending Machines, dumpster, etc. More Flavor.
DONE >> Setup a Graphics Settings menu with options for things like resolution, quality, etc.
DONE >> Security Guards
DONE >> Fencing for outside areas with barbed wire on top
DONE >> gate for truck entrance
DONE >> turnstiles for employee entrance
DONE >> guard shack
DONE >> drivers lounge
DONE >> Stairs that lead to yard
DONE >> Vending Machines
DONE >> Popups for showing information about objects in the world when you hover over them. (e.g. when you hover over a building, it shows the name and cost of the building)
DONE >> Grid changed from 60x60 to 100x100.
DONE >> Waypoints look ugly. They're glow in the dark. I just want regular albedo 's. 
DONE >> Guidance line looks like shit - what happened
DONE >> Get angent back on top of surfaces
DONE >> Slow down footsteps and fuck with spatial volume
DONE >>Get agent ghost preview to follow raytrace
DONE >> Get agents to pathfind around objects in the way of their path.
DONE >> If the question mark is floating over the agents head for a certain amount of time let's say three seconds or more A rebake of the navmesh surface should automatically take place it's that way we can			keep checking to try to get the navagant's moving again Would that be a performance hit A significant performance hit anyways? 
DONE >> Add back in the floor prefabs to the foundation object 
DONE >> But Navagents don't appear to be casting shadows What's up with that can you make them cast shadows please soft shadows ?
DONE >>Missing Materials on vending machines
DONE >> Implement the Turn animations back into the Agent animation script
DONE >>Find scripts that are parenting objects in hierarchy automatically I can't see what's in the scene There are no clones I would like for a script that possibly parents categories of objects under emp empty game objects in the hierarchy such as floor tiles or flavor or whatever 
DONE >> Fix Thumbnails - Need a thumbnail added 4 yard ground . Need a thumbnail for Yard tile under floors Category.  
DONE >> Make new benches amd office assets
DONE >> Camera is back to old one - need the one like in the SIms 4
DONE >> Update the script that will allow me to download a prefab and some kind of app UI interface where I can just drag that model into a serialized game object field and have drop downs that will allow me to assign category name  And then automatically create prefabs inside they prefab category hook it up to the Objectso registry as well as the bottom bar UI category items automatically assign a unique key set up all the components set up box Collider and anything else that might be needed for that category to speed up production process and possibly to be used in later projects 
DONE >> On save/load ui - the keystrokes are registering through the UI and making the camera move.
DONE >> DevTools is not letting me click the checkboxes when game objects are behind it.
DONE >> Add the Blender shader - moving to bottom - CM is slow as fuck
DONE >> Break area furniture (e.g. tables, chairs, etc.)
DONE >> Make all UIs match Artic Blaze aesthetic 
DONE >> Let's make it so that if I change resolutions the UI elements always automatically scale with the size of the frame of desktop settings Or whatever resolution we're 
DONE >> Beef Up UIs with the Kits I downloaded and make them look more visually appealing and consistent with the overall art style of the game. This includes things like the save/load windows, the build menu, the employee stats display, etc.
DONE >> IC Employees
Done >> Truck Drivers
DONE >> Jump down implementer
DONE >> Climb up implemented
DONE >> Create a low poly truck model and add it to the game. It should be able to drive around the yard and back into the shipping doors when a delivery is scheduled.
DONE >> Stat displays for employees (e.g. when you click on an employee, it shows their stats such as speed, strength, etc.)  
DONE >> Implement chat bubbles and reaction emoticons over their heads.  
DONE >> Make sure terminated employees feet are touching ground when they leave They were hovering at about 1 Meter so I don't think it's updating the Y coordinate when we terminate them. Also can we have some kind of lightning bolt graphic plane as they head out the door?
DONE >> Need a save method so all avatars are persistent through saves.
DONE >> Shift clicking an employee  info UI We'll bring them on the employee roster already selected.
DONE >> Clicking on an employee in the world will create a Light blue outline so you can see what is selected and see them either in the employee info UI and Oregon the roster
DONE >> Man-door glass entrance not being allowed to be placed inside a single cell that is by other walls Not sure why Please address.
DONE >> Bring back the old low Poly. Need more dramatic shadows in game.
DONE >> On shipping doors if door green outside it needs to be red inside and vice versa..
DONE >>Red lights on are not on.
DONE >> Do not bring up the pallet builder window if we are in build mode
DONE >> You cannot Delete floor tiles that are on foundations only replace them you cannot ever delete floor tiles only replace them you cannot move them You can only replace them. That being said when a tile is on top of a foundation the rule is the foundation underneath the tile gets deleted along with all four tiles that were sitting on top of it. This will allow you to place a new foundation if you absolutely feel like you need to get rid of the foundation. But that should not happen very often.
DONE >> Explore options for employee could we create GIF, Could we have animation loops play. Or should we just have flat straight on photos like a drivers license question? If it's too cost performant we could always just have the animated ones for the higher level of graphics like Ultra
DONE >> Set up the employee photo. Should probably be below zero so user cannot. We'll need a special dedicated camera down there Lights backdrop of some sort could be random.
DONE >> Create the employee photo booth
DONE >>Employee photo and appearance are assigned to employee in employee data..Cs or whatever file holds them. I should probably figure that out

06-14
Create script that picks customizable appearance options like hair, accessories and colors then instantiates them on avatar and finalizes them.











